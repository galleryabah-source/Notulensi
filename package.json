{
  "name": "meeting-intelligence-ultimate-runtime",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Minimal server runtime foundation for Meeting Intelligence Ultimate. Existing HTML application remains unchanged.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "db:migrate": "node server/db/migrate.mjs",
    "runtime:self-test": "node server/runtime-self-test.mjs",
    "security:integration-test": "node server/security-repository.integration-test.mjs",
    "security:endpoint-contract-test": "node server/security/protected-endpoint-contract.self-test.mjs",
    "security:regression-test": "node server/security/security-integration-regression.self-test.mjs",
    "security:pilot-test": "node server/security/controlled-endpoint-pilot.self-test.mjs",
    "security:registry-test": "node server/security/protected-route-registry.self-test.mjs",
    "security:audit-test": "node server/security/security-audit-event.self-test.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
