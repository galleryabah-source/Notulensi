{
  "name": "meeting-intelligence-ultimate-runtime",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Minimal server runtime foundation for Meeting Intelligence Ultimate. Existing HTML application remains unchanged.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "db:migrate": "node server/db/migrate.mjs",
    "runtime:self-test": "node server/runtime-self-test.mjs",
    "security:integration-test": "node server/security-repository.integration-test.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
