(() => {
  'use strict';
  const store = window.notulensiLiteStorage;
  const recorder = window.NotulensiLiteRecorder;
  const transcription = window.NotulensiLiteTranscription;
  if (!store || !recorder || !transcription) throw new Error('NOTULENSI_LITE_ENGINE_UNAVAILABLE');
  const $ = (id) => document.getElementById(id);
  const esc = (s) => String(s ?? '').replace(/[&<>'"]/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const state = { sessions:[], version:0, backend:false, recording:false, paused:false, recordingId:null, durationMs:0, finalizing:false, dirty:false };
  function localLoad(){const p=store.read();state.sessions=Array.isArray(p.sessions)?p.sessions:[];state.version=Number(p.version)||0;}
  function localSave(){return store.write({sessions:state.sessions,version:state.version});}
  async function api(path,opt={}){const r=await fetch(path,{credentials:'same-origin',cache:'no-store',...opt,headers:{...(opt.body?{'Content-Type':'application/json'}:{}),...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(d.error||`HTTP_${r.status}`),{status:r.status,data:d});return d;}
  async function hydrate(){try{const a=await api('/api/admin-login.js');if(!a.authenticated){setMessage('Mode lokal aktif. Login admin diperlukan untuk sinkronisasi cloud.');return;}const d=await api('/api/lite-data.js');if(!Array.isArray(d.data?.sessions))return;state.sessions=d.data.sessions;state.version=d.version;state.backend=true;await recoverAudioLinks();renderHistory();setMessage('Sinkronisasi cloud aktif.');}catch{state.backend=false;setMessage('Mode lokal aktif. Login admin diperlukan untuk sinkronisasi cloud.');}}
  async function recoverAudioLinks(){await Promise.all(state.sessions.map(async s=>{if(!s.recordingId)return;try{s.audioAvailable=Boolean((await recorder.getRecording(s.recordingId))?.blob);}catch{s.audioAvailable=false;}}));localSave();}
  async function persist(){localSave();if(!state.backend)return true;try{const d=await api('/api/lite-data.js',{method:'PUT',headers:{'X-Notulensi-Lite-Request':'1'},body:JSON.stringify({baseVersion:state.version,data:{sessions:state.sessions}})});if(d.conflict){state.sessions=d.data?.sessions||[];state.version=d.version;localSave();setMessage('Versi server berubah. Data server dipertahankan untuk mencegah overwrite.');return false;}state.version=d.version;return true;}catch(e){setMessage(e.status===401?'Sesi admin berakhir. Data tetap tersimpan lokal.':'Sinkronisasi cloud gagal. Data tetap tersimpan lokal.');return false;}}
  function setMessage(x){$('message').textContent=x;}
  function setStatus(x,live=false){$('status').textContent=x;$('dot').classList.toggle('live',live);}
  function fmt(ms){return recorder.formatMs(ms);}
  function renderTimer(e){$('timer').textContent=e.display||fmt(e.elapsed||0);state.durationMs=e.elapsed||0;}
  function renderHistory(){const h=$('history');if(!state.sessions.length){h.innerHTML='<div class="empty">Belum ada sesi tersimpan.</div>';return;}h.innerHTML=state.sessions.map(s=>`<article class="history-item"><div class="history-main"><strong>${esc(s.title||'Tanpa judul')}</strong><span>${esc(new Date(s.createdAt||Date.now()).toLocaleString('id-ID'))}</span></div><div class="muted">${fmt(s.durationMs||0)} · ${s.audioAvailable?'audio lokal tersedia':'transkrip saja'}</div><p>${esc(s.text||'').slice(0,360)}</p><div class="history-actions"><button class="ghost" data-load="${esc(s.id)}">Buka</button>${s.audioAvailable?`<button class="ghost" data-audio="${esc(s.recordingId)}">Unduh Audio</button>`:''}</div></article>`).join('');}
  function setControls(){const r=state.recording;$('start').disabled=r||state.finalizing;$('pause').disabled=!r||state.paused||state.finalizing;$('resume').disabled=!r||!state.paused||state.finalizing;$('stop').disabled=!r||state.finalizing;$('save').disabled=state.finalizing||r||!$('text').value.trim()||!state.dirty;}
  function refreshSupport(){const audio=!!window.MediaRecorder&&!!navigator.mediaDevices?.getUserMedia,speech=transcription.support();$('support').innerHTML=`<span class="support-chip ${audio?'ok':'bad'}">${audio?'● Audio siap':'● Audio tidak tersedia'}</span><span class="support-chip ${speech?'ok':'warn'}">${speech?'● Transkrip siap':'● Transkrip browser terbatas'}</span>`;}
  async function start(){try{if(state.recording||state.finalizing)return;transcription.reset('');state.recordingId=null;state.durationMs=0;await recorder.start();state.recording=true;state.paused=false;state.dirty=false;setStatus('Sedang merekam',true);setMessage('Mikrofon aktif.');setControls();try{await transcription.start();}catch{setMessage('Audio aktif; transkripsi browser tidak tersedia. Rekaman tetap berjalan.');}}catch(e){setStatus('Siap merekam');setMessage(e.message==='MICROPHONE_API_UNAVAILABLE'?'Browser tidak menyediakan akses mikrofon.':'Akses mikrofon gagal. Periksa izin browser lalu coba lagi.');}}
  function pause(){if(!state.recording)return;recorder.pause();state.paused=true;transcription.stop();setStatus('Rekaman dijeda');setMessage('Rekaman dijeda.');setControls();}
  async function resume(){if(!state.recording||!state.paused)return;recorder.resume();state.paused=false;try{await transcription.start();}catch{}setStatus('Sedang merekam',true);setMessage('Rekaman dilanjutkan.');setControls();}
  async function stop(){if(!state.recording||state.finalizing)return;state.finalizing=true;setControls();try{transcription.stop();const meta=await recorder.stop();state.recording=false;state.paused=false;state.recordingId=meta?.id||null;state.durationMs=meta?.durationMs||state.durationMs;setStatus('Rekaman selesai');setMessage('Rekaman selesai. Tinjau transkrip lalu simpan.');}catch{state.recording=false;state.paused=false;setStatus('Rekaman selesai');setMessage('Audio gagal difinalisasi; transkrip tetap dapat disimpan.');}finally{state.finalizing=false;setControls();}}
  async function save(){if(state.recording||state.finalizing){setMessage('Hentikan rekaman terlebih dahulu.');return;}const text=$('text').value.trim();if(!text){setMessage('Belum ada transkrip untuk disimpan.');return;}const title=$('title').value.trim()||`Sesi ${new Date().toLocaleString('id-ID')}`;let audio=null;if(state.recordingId)audio=await recorder.getRecording(state.recordingId).catch(()=>null);const session={id:`s_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,title,text,durationMs:state.durationMs,createdAt:new Date().toISOString(),recordingId:audio?.id||state.recordingId||null,audioAvailable:Boolean(audio?.blob)};state.sessions.unshift(session);const synced=await persist();if(synced||!state.backend){state.dirty=false;renderHistory();setControls();setMessage(session.audioAvailable?'Sesi dan audio tersimpan.':'Transkrip tersimpan.');}}
  function resetEditor(){if(state.recording||state.finalizing){setMessage('Hentikan rekaman terlebih dahulu.');return;}transcription.reset();$('title').value='';$('text').value='';state.recordingId=null;state.durationMs=0;state.dirty=false;renderTimer({elapsed:0,display:'00:00:00'});setMessage('Sesi baru siap.');setControls();}
  function copyText(){const text=$('text').value;if(!text){setMessage('Belum ada transkrip.');return;}navigator.clipboard?.writeText(text).then(()=>setMessage('Transkrip disalin.')).catch(()=>setMessage('Clipboard tidak tersedia.'));}
  function downloadText(){const u=URL.createObjectURL(new Blob([$('text').value],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=u;a.download=`notulensi-lite-${Date.now()}.txt`;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000);}
  window.addEventListener('notulensi:lite:timer',e=>renderTimer(e.detail));
  window.addEventListener('notulensi:lite:stopped',e=>{state.recordingId=e.detail.id;state.durationMs=e.detail.durationMs||state.durationMs;setControls();});
  window.addEventListener('notulensi:lite:transcription:result',e=>{$('text').value=e.detail.combined||'';state.dirty=true;setControls();});
  window.addEventListener('notulensi:lite:transcription:error',e=>{if(state.recording)setMessage(`Transkripsi: ${e.detail.code||'error'}. Rekaman audio tetap berjalan.`);});
  $('start').onclick=start;$('pause').onclick=pause;$('resume').onclick=resume;$('stop').onclick=stop;$('save').onclick=save;$('copy').onclick=copyText;$('download').onclick=downloadText;$('clear').onclick=resetEditor;$('text').oninput=()=>{state.dirty=true;setControls();};$('title').oninput=()=>{state.dirty=true;setControls();};
  $('history').onclick=async e=>{const b=e.target.closest('[data-load],[data-audio]');if(!b)return;if(b.dataset.load){const s=state.sessions.find(x=>x.id===b.dataset.load);if(s){$('title').value=s.title||'';$('text').value=s.text||'';state.recordingId=s.recordingId||null;state.durationMs=s.durationMs||0;state.dirty=false;renderTimer({elapsed:state.durationMs,display:fmt(state.durationMs)});setMessage('Sesi dibuka untuk ditinjau.');setControls();}}else{try{recorder.download(await recorder.getRecording(b.dataset.audio));}catch{setMessage('Audio lokal tidak ditemukan.');}}};
  window.addEventListener('beforeunload',e=>{if(state.recording||state.dirty){e.preventDefault();e.returnValue='';}});
  localLoad();refreshSupport();renderTimer({elapsed:0,display:'00:00:00'});setControls();renderHistory();hydrate();
})();
