# Security repositories

`security-repository.mjs` is the PostgreSQL implementation of the Phase 4.22 runtime repository contract.

It is intentionally not imported by the browser application yet. Integration into request authorization is a later gated phase.
